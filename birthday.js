document.addEventListener('DOMContentLoaded', function() {
  const cardContent = document.querySelector('.card-content');
  cardContent.style.opacity = '1'; // Ensuring content is visible even if animation fails
});
